import { Meta } from '@storybook/blocks';

<Meta title="Templates/Overview" />

# Templates

Starting points for entire UI screens. Each template composes existing components into a
realistic page layout that can be used as a reference or copy-pasted into an application.

| Template | Description |
|----------|-------------|
| **App Shell** | Sidebar navigation + top bar + scrollable content area |
